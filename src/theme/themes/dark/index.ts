import { MyDefaultTheme } from 'src/theme';

/**
 * The default colors.
 */
const defaultColors = {
    border: '#ededed',
    primary: '#BD4B4B'
};

/**
 * The dark theme.
 */
export const darkTheme: MyDefaultTheme = {
    colors: defaultColors,
    global: {
        radius: '7px'
    }
};