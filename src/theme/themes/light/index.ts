/**
 * The default colors.
 */
const defaultColors = {
    border: '#ededed',
    primary: '#BD4B4B'
};

/**
 * The light theme.
 */
export const lightTheme = {
    colors: defaultColors,
    global: {
        radius: '7px'
    }
};